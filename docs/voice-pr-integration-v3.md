# #3149 语音整合基线

本分支整合来源 PR，并保留独立 ASR 准入、异常恢复、采样归属和可靠收尾修复；不再是仅包含 merge 的分支。

| 来源 | 本次包含的 head |
| --- | --- |
| upstream/main | babfd763f7cc2b9512295ff8406895b9d832c9d9 |
| #3078 | 已以 a5dff0834 合入 main；原 head ff546a086 也在整合历史中 |
| #3089 | 1f3529d673b3edbe928de75c172c78a8ee8f1f4a |
| #3103 | 1dc41ff2332ad6f2b8fa3da09d75d445bc77d8df |
| #3130 | 6548ed9dd221da81b49711a8e77c00fc4ac389a7 |

## 本次整理

保留已有历史和独立修复，不重复应用 #3078 的 squash 内容；其原 head 已完整包含，原 head 与 squash 的其他差异也已由旧主线基线包含。同步主线后续 #3068、#3146 增量并合并语言缓存版本。

纳入 #3103 的 40ms 唤醒批量通信、30ms 主动等待、处理确认及尾帧续接。模型继续常驻并逐帧检测；没有更改阈值或更新 wheel。

纳入 #3089 的 TTS 容量释放后重试、配置 fallback 恢复、ASR backpressure 单次结算和重复启动请求失败通知。冲突处理保留 #3149 的准入证据清理与无 voice handoff ticket 时的原关闭路径。

## 保留的独立修复

- 独立 ASR 候选准入和转写兜底；NEKO_ASR_ADMISSION=1 在新会话启用，默认关闭。
- 明确故障的一次结算、有界恢复及 Qwen 显式 finish_and_drain；不新增自动强制断句。
- 16kHz 采样范围与跨窗口交接归属；真实缺失、乱序和不确定交付继续保护性停止。
- Qwen、Step、OpenAI 物理连接可靠收尾，重复取消不跳过清理；保留原始故障分类，不用激活保护标记覆盖恢复终态。
- 浏览器 AGC 关闭及按 PCM 时长修正项目 AGC 时间常数；不在本次整理中调整参数。

## 验证与边界

2026-09-24 整合树的 387 个定向测试通过，覆盖唤醒、交接、TTS 容量、ASR 故障分类、连接退休、采样归属和语言缓存；Ruff、异步阻塞及 Core 契约检查通过。未跑全量测试。

#3103 的正式 runtime 单组测量是来源 PR 的证据，不能当作本整合环境已完成实测。网页、Electron、真实麦克风和游戏高负载仍需人工验收。此次未重启正在运行的后端。

环境音被模型持续判为人声、短词转写准确率和云端长期不判结束仍是独立问题。入口拒绝前已上传的音频不能追回费用；不确定交付不自动重传，partial 不作为 final，用户关麦不能被恢复覆盖。
