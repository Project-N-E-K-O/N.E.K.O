# 本地版 u1 — 改动说明

按路径归类（对应本 PR 实际提交；**不含** `vendor/rvc` 第三方大树、QQ / 战雷无关改动、密钥）。

## 文档

- `docs/local-u1/` — 本说明包（改造列表 / 改动说明 / 环境补齐）

## 双语与 TTS

- `utils/bilingual_speech.py` — 双标签拆分与脚本守卫
- `utils/tts_japanese_gate.py` — TTS 前日语确保
- `main_logic/core/turn.py` / `manager.py` / `tts_runtime.py` / `notify.py` / `proactive.py` / `greeting.py`
- `main_logic/omni_offline_client/_lifecycle.py` 等 — 流式/离线路径衔接
- `config/prompts/prompts_chara.py` 等 — 角色提示配合双语言
- `tests/unit/test_bilingual_speech.py` / `test_tts_japanese_gate.py`

## 人格 / 称呼

- `utils/config_manager/persona_payload.py`
- `utils/config_manager/characters.py` / `core_config.py`（相关接线）

## RVC 翻唱插件

- `plugin/plugins/rvc_cover/**` — 插件本体、面板、i18n、文档
- `main_logic/music_playback.py` / `music_requests.py` — `/runs` 触发与播放衔接
- `scripts/setup_rvc_vendor.ps1` / `sync_rvc_weights.ps1` / `start_rvc_training.*`
- `vendor/rvc/README.md` — 仅说明如何本地同步（源码/runtime 不进本 PR）
- `tests/unit/test_rvc_cover_plugin.py` / `test_rvc_web_process.py`

## 本地桌面与 ASR

- `start_desktop.bat` / `start_desktop_silent.bat` / `start_desktop_silent.vbs`
- `set_local_ai.bat` / `scripts/set_local_ai.py` / `scripts/edge_tts_bridge.py`
- `install_env.bat` / `install_local_asr.bat` / `setup_and_build.bat` 等辅助脚本
- `main_logic/asr_client/workers/faster_whisper.py` 及 asr_client 相关接线
- `tests/unit/test_asr_faster_whisper_worker.py` 等

## 前端 / 静态资源

- `frontend/plugin-manager/src/**` — 插件列表/详情/启停相关
- `static/app/*` / `static/avatar/*` / `static/live2d/*` / `static/locales/*` — 交互与文案配合

## 配置忽略

- `.gitignore` — 忽略 `vendor/rvc/**`（保留 README）、本地模型缓存、插件 runtime 状态与个人 profiles
