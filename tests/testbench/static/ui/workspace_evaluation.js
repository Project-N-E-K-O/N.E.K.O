/**
 * workspace_evaluation.js — Evaluation workspace 入口.
 *
 * P03 占位. 后续 (P15/P16/P17) 会挂 Run / Results / Aggregate / Schemas 四子页.
 */

import { renderPlaceholderWorkspace } from './workspace_placeholder.js';

export function mountEvaluationWorkspace(host) {
  renderPlaceholderWorkspace(host, 'evaluation');
}
