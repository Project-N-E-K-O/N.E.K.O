/**
 * workspace_settings.js — Settings workspace 入口.
 *
 * P03 占位. 后续 (P04) 会挂 Models / API Keys / Providers / UI / About 五子页.
 */

import { renderPlaceholderWorkspace } from './workspace_placeholder.js';

export function mountSettingsWorkspace(host) {
  renderPlaceholderWorkspace(host, 'settings');
}
